from setuptools import setup

setup(name='IRP_ty221',
      version='1.0',
      description='IRP_package',
      author='Tianshun',
      packages=['tools'],
      package_dir={'tools': 'tools'},
      )
